# Minimal Homelab Example

This is a synthetic example. It does not describe a real private network.

The example has four surfaces:

- `source`: the Git repository
- `host`: a fake host checkout
- `runtime`: a fake runtime export
- `live-config`: fake mutable runtime config

Use it to test documentation, schemas, and receipt examples without exposing
private infrastructure.
