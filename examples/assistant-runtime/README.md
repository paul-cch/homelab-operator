# Assistant Runtime Example

This synthetic example shows an assistant-like runtime split into source,
runtime export, and live config.

It is not a deploy tool. It is an example of how to phrase proof boundaries when
source changes require later runtime verification.
